import { FilterByBranchPipe } from './filter-by-branch.pipe';

describe('FilterByBranchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBranchPipe();
    expect(pipe).toBeTruthy();
  });
});
